spring:
  application:
    name: VehiclePartsPro
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost:8180/realms/VehiclePartsPro/protocol/openid-connect/certs
          issuer-uri: http://localhost:8180/realms/VehiclePartsPro

  datasource:
    url: jdbc:mysql://localhost:3306/PezziDiRicambio
    username: root
    password: Roccopio2.
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        show_sql: true
        format_sql: true


jwt:
  auth:
    converter:
      resource-id: ${KEY_CLOAK_RESOURCE:User_VPP_id}
      principal-attribute: preferred_email
